Porta mapeada do banco ficou com 9001:5432
Porta mapeada a ser implementada do próprio spring 9002:8080 - mas ainda será implementada, o método no compose pra isso é o que está em comentário


Modelo de commits a serem usados 

git checkout -b 01-mudando-alguma-coisa

git checkout -b 02-mudando-coisa-alguma

por aí vai, seguindo ordem numérica e nome da branch especificando o que está editando/codando

depois de criar essa branch usando git checkout (tu cria a branch localmente mas temos que mandar pra o repositório remoto)

lembrar de usar

git push -u origin nome-da-branch para jogoar a branch no repositório remoto

para iniciar o servidor do backend é só utilizar comnando no diretório /backend - npm run dev
